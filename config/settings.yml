user:
  name:
    max_length: 50
  email:
    max_length: 255
    valid_email_regex: !ruby/regexp /\A[\w+\-.]+@[a-z\d\-.]+\.[a-z]+\z/i
  password:
    min_length: 6
  image:
    size: 80
  per_page: 10
  micropost_per_page: 5
  image_max_size: 5
  validate_content: 140
  folow_per_page: 10
